# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# 환경변수·키 파일 (API 키 — 절대 커밋 금지)
.env
secrets/
*service-account*.json

# 로그·생성물 (개인정보 보호: 발화 로그는 올리지 않음)
data/logs/
data/tts_cache/
reports/expression_candidates.csv
data/testset_augmented.jsonl

# OS
.DS_Store
Thumbs.db

# 이미지 원본 백업 (최적화 전 대용량 — 커밋 안 함)
rsc/product-originals/
rsc/banner-originals/
